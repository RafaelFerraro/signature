require 'spec_helper'

describe Signature do
  it 'has a version number' do
    expect(Signature::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
